import { shallow, mount, render } from 'enzyme';
import React from 'react';
import Card from './card.components';

it('expexts to render card component', () => {
    // const mockMonsters = [
    //     {
    //         "id": 1,
    //         "name": "Leanne Graham",
    //         "username": "Bret",
    //         "email": "Sincere@april.biz"     
    //     }
    // ]
    // expect(shallow(<Card monster={mockMonsters}/>).length()).toEqual(1);
})